// src/benchmarks/go-mode-benchmarks.js
// Stub généré automatiquement pour rétablir la CI MONSTERDOG.
// Ce module sera remplacé plus tard par le moteur complet GO Mode.

// Liste vide pour le moment — permet à la pipeline de charger sans erreur.
module.exports = {
  benchmarks: [],

  // Fonction minimale attendue par l'orchestrateur.
  getAll: function () {
    return this.benchmarks;
  }
};
